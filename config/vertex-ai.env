# Google Vertex AI Configuration for Claude Code
# ===============================================

# Enable Vertex AI integration with Claude Code
CLAUDE_CODE_USE_VERTEX=1

# Recommended region for Claude models (best availability)
CLOUD_ML_REGION=us-east5

# Your Google Cloud Project ID
# Replace with your actual project ID
ANTHROPIC_VERTEX_PROJECT_ID=your-gcp-project-id

# Optional: Custom Vertex AI endpoint (if using private deployment)
# ANTHROPIC_VERTEX_BASE_URL=https://custom-endpoint.googleapis.com

# Authentication method (choose one):
# 1. Application Default Credentials (recommended)
#    Run: gcloud auth application-default login
# 2. Service Account Key File
#    Set: GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Model Configuration
# Primary model (high-performance tasks)
VERTEX_PRIMARY_MODEL=claude-sonnet-4@20250514

# Secondary model (fast tasks)
VERTEX_SECONDARY_MODEL=claude-3-5-haiku@20241022

# Cost and Usage Monitoring
ENABLE_VERTEX_COST_TRACKING=true
VERTEX_COST_ALERT_THRESHOLD=100.0  # USD
VERTEX_USAGE_LOG_FILE=/tmp/vertex-usage.log

# Rate Limiting
VERTEX_MAX_REQUESTS_PER_MINUTE=100
VERTEX_MAX_TOKENS_PER_MINUTE=100000

# Debug and Logging
ANTHROPIC_LOG=info  # debug, info, warn, error
VERTEX_DEBUG_MODE=false